import { inView } from "framer-motion";

export default function ContactTop(){
    return(
        <div className="bg-[url('/contacttop.gif')] md:bg-[url('/contacttop.gif')] bg-center bg-cover w-full max-w-full h-screen mt-0 flex justify-center items-center">
            <h1 className="text-white text-3xl sm:text-4xl md:text-6xl lg:text-8xl font-semibold">CONNECT WITH US</h1>
        </div>
    )
}